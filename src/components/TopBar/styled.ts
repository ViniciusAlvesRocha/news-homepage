import styled from 'styled-components';

const TopBarStyled = styled.header`
    nav {
        ul {
            li {
                margin-left: 30px;
            }
        }
    }
`;

export default TopBarStyled;